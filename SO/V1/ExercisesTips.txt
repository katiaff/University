OS_LongTermScheduler(){
	...

	PID = OS_CreateProcess(...);
	if (error)
		show error message;
}

int OS_CreateProcess(...){
	...
	
	PID = OS_ObtainEntryProcessTable();
	if (error) //PID is negative
		return ERROR_CODE;
	else
		continue below;
}
