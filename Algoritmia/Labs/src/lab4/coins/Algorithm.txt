1) Check if fake coin is weight-1 | weight + 1
2) 
   If it is weight-1:
       choose LEFT on balance
   If it is weight+1:
       choose RIGHT on balance
3) If total length odd:
       leftSideEnds = rightSideStarts
	
	